package com.svennieke.statues.packets;

public class StatuesAFKPacket //implements IMessage{
{
	/*
	 * public boolean isAfk;
	 * 
	 * public StatuesAFKPacket() {}
	 * 
	 * public StatuesAFKPacket(boolean afk) { this.isAfk = afk; }
	 * 
	 * @Override public void fromBytes(ByteBuf buf) { isAfk = buf.readBoolean(); }
	 * 
	 * @Override public void toBytes(ByteBuf buf) { buf.writeBoolean(isAfk); }
	 * 
	 * public static class PacketHandler implements
	 * IMessageHandler<StatuesAFKPacket, IMessage> {
	 * 
	 * @Override public IMessage onMessage(StatuesAFKPacket message, MessageContext
	 * ctx) { FishHandler.updateAfk(ctx.getServerHandler().player, message.isAfk);
	 * return null; } }
	 */
}
